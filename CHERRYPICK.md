# Cherry-Pick

369babe on `lab09-feature` and 3f763a2 on `lab09-hotfix` contain the exact same change, but are two different commits with two different hashes. `git cherry-pick` copies a commit, it does not move it.
