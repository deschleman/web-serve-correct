# Cherry-Pick

`d24f02f` on `lab09-feature` and `7b81310` on `lab09-hotfix` contain the exact same change, but are two different commits with two different hashes. `git cherry-pick` copies a commit, it does not move it.
